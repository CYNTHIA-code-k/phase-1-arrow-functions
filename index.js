//
const divide = (a, b) => {
  return a / b;
};

let a = 2000;
let b = 100;

console.log(divide(a, b)); 

// Defining the square arrow function

const square = (num) => {
  return num * num;
};

let x = 2;

console.log(square(x)); 

const add = (A, B) => {
  return A + B;
};

// 
let A = 3;
let B = 4;

// 
console.log(add(A, B)); 
